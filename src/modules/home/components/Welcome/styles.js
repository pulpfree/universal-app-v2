import { StyleSheet } from 'react-native'

// import clr from '../../../../config/colors'

export default StyleSheet.create({
  view: {
    marginBottom: 15,
    paddingBottom: 10,
  },
  text: {
    fontSize: 18,
    fontWeight: '600',
  },
})
